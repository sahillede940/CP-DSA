#include <iostream>
using namespace std;

int b_S(int *a, int s, int e, int k)
{

    if (x > e)
    {
        return -1;
    }
    else if (a[mid])
}

int main()
{
    int a[10] = {1, 2, 3, 4, 5, 8, 9, 11, 23, 32};
    int k;
    cin >> k;
    cout << b_S(a, 0, 9, k);
    return 0;
}